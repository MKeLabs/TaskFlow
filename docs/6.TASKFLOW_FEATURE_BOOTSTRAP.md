# Step 6: App Bootstrap & Root Layout

This step covers the main app configuration, root component, and layout.

## What & Why

This step wires up the main app configuration and root layout. It shows how to bootstrap your Angular app, set up routing, and provide a consistent layout and navigation. This is the foundation that ties all features together and makes the app work as a whole.

## 1. App Config (`src/app/app.config.ts`)
```typescript
import { ApplicationConfig, provideZoneChangeDetection } from '@angular/core';
import { provideRouter } from '@angular/router';
import { provideHttpClient, withInterceptors } from '@angular/common/http';

import { routes } from './app.routes';
import { authInterceptor } from './core/auth/auth.interceptor';

export const appConfig: ApplicationConfig = {
  providers: [
    provideZoneChangeDetection({ eventCoalescing: true }),
    provideRouter(routes),
    provideHttpClient(withInterceptors([authInterceptor]))
  ]
};
```

## 2. Main Entry (`src/main.ts`)
```typescript
import { bootstrapApplication } from '@angular/platform-browser';
import { appConfig } from './app/app.config';
import { AppComponent } from './app/app.component';

bootstrapApplication(AppComponent, appConfig)
  .catch((err) => console.error(err));
```

## 3. Root Component TypeScript (`src/app/app.component.ts`)
```typescript
import { Component, computed, inject } from '@angular/core';
import { Router, RouterLink, RouterLinkActive, RouterOutlet } from '@angular/router';
import { AuthService } from './core/auth/auth.service';

@Component({
  selector: 'app-root',
  standalone: true,
  imports: [RouterOutlet, RouterLink, RouterLinkActive],
  templateUrl: './app.component.html',
  styleUrl: './app.component.scss'
})
export class AppComponent {
  title = 'taskflow-web';
  private readonly auth = inject(AuthService);
  private readonly router = inject(Router);
  readonly isAuthenticated = computed(() => this.auth.isAuthenticated());

  async logout() {
    this.auth.logout();
    await this.router.navigateByUrl('/login');
  }
}
```

## 4. Root Component HTML (`src/app/app.component.html`)
```html
<div class="shell">
  <header class="topbar">
    <div class="brand">TaskFlow</div>
    <div class="topbar-actions">
      <nav class="nav">
        <a routerLink="/projects" routerLinkActive="active">Projects</a>
        <a routerLink="/login" routerLinkActive="active">Login</a>
      </nav>
      @if (isAuthenticated()) {
        <button class="secondary" type="button" (click)="logout()">Logout</button>
      }
    </div>
  </header>

  <main class="content">
    <router-outlet />
  </main>
</div>
```

## 5. Root Component SCSS (`src/app/app.component.scss`)
```scss
.shell {
  min-height: 100vh;
  background: #0b1220;
  color: #e5e7eb;
}

.topbar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 12px 18px;
  border-bottom: 1px solid rgba(255, 255, 255, 0.08);
  background: rgba(255, 255, 255, 0.03);
  backdrop-filter: blur(8px);
}

.brand {
  font-weight: 700;
  letter-spacing: 0.2px;
}

.nav {
  display: flex;
  gap: 12px;

  a {
    color: #e5e7eb;
    text-decoration: none;
    padding: 6px 10px;
    border-radius: 10px;
    border: 1px solid rgba(255, 255, 255, 0.08);
    background: rgba(255, 255, 255, 0.02);
  }

  a.active {
    border-color: rgba(99, 102, 241, 0.5);
    background: rgba(99, 102, 241, 0.15);
  }
}

.topbar-actions {
  display: flex;
  align-items: center;
  gap: 10px;
}

button.secondary {
  padding: 6px 10px;
  border-radius: 10px;
  border: 1px solid rgba(255, 255, 255, 0.14);
  background: rgba(255, 255, 255, 0.06);
  color: #e5e7eb;
  cursor: pointer;
}

.content {
  max-width: 960px;
  margin: 0 auto;
  padding: 18px;
}
```

---

You now have a complete, working Angular SPA UI for your API!
